{
  "name": "uci-syllabus-finder-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "turbo build",
    "deploy": "turbo deploy",
    "scrapers:test": "turbo scrapers:test",
    "db:pull": "turbo db:pull",
    "db:generate": "turbo db:generate",
    "lint": "turbo lint",
    "dev": "turbo dev",
    "preview": "turbo preview"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "^1.10.16",
    "typescript": "^5.3.2"
  }
}
